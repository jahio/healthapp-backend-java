package healthapp;

import io.ebean.config.DatabaseConfig;
import io.javalin.http.Handler;
import io.ebean.Database;

public class BPController {
    Database db = new Database(new DatabaseConfig())
    public static Handler between = ctx -> {

    }


}
